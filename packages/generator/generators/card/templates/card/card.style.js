import { withStyles } from '@material-ui/styles';

export default withStyles((theme) => {
  return {
    main: {
      // marginTop: '55px',
      // display: 'block',
      // [theme.breakpoints.up('md')]: {
      //   padding: '60px 0',
      // },
    },
  };
});
